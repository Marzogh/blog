/**
 * Deprecated (legacy location).
 *
 * Astro v5 uses `src/content.config.ts` as the canonical content collections file.
 * This file is kept only as a compatibility shim in case any older imports
 * still reference `src/content/config.ts`.
 *
 * Do not define collections here.
 */

export { collections } from '../content.config';